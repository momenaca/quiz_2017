<h1>
    Pistas pendientes de ser aceptadas:
</h1>

    <% if(tips.length !=0 && quizzes.length !=0) {%>
    <% for (var i in tips) { %>
    <% var tip = tips[i]; %>
    <% var quizId = quizzes[tip.QuizId-1]%>

    <li>
        <%= tip.text %>
        <span id="autor"> (by <%= tip.Author ? tip.Author.username : "Anónimo" %>) </span>

        <a href="/quizzes/<%= quizId %>/tips/<%= tip.id %>/accept?_method=PUT">
            <button>Aceptar</button>
        </a>

        <a href="/quizzes/<%= quizId %>/tips/<%= tip.id %>?_method=DELETE">
            <button>Borrar</button>
        </a>

    </li>
    <% } %>
<% } %>

    <% if(tips.length == 0) {%>
    <p>
        No hay ninguna pista pendiente de ser aceptada.
    </p>
    <% } %>

</ul>
